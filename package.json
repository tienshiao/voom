{
  "name": "voom",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build ./src/index.html --outdir=dist --sourcemap --target=browser --minify --define:process.env.NODE_ENV='\"production\"' --env='BUN_PUBLIC_*'",
    "start": "NODE_ENV=production bun src/index.ts",
    "build:voom": "bun run build-single.ts",
    "build:voom:all": "bun run build-all.ts",
    "install:skill": "mkdir -p ~/.claude/commands && cat > ~/.claude/commands/voom.md << 'EOF'\nPlease run the `voom` command to collect code review feedback. The command is user interactive, do not use a timeout.\nReview the feedback and make necessary changes to the code to address the feedback.\nEOF"
  },
  "dependencies": {
    "lucide-react": "^0.562.0",
    "react": "^19",
    "react-dom": "^19",
    "react-markdown": "^10.1.0"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/bun": "latest"
  }
}
